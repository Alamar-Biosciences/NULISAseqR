% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/writeNULISAseq.R
\name{add_sheet_to_wb}
\alias{add_sheet_to_wb}
\title{Add a Sheet to an Excel Workbook with Configurable NA Handling}
\usage{
add_sheet_to_wb(
  wb,
  sheet_name,
  data,
  colNames = TRUE,
  na_as_string = FALSE,
  startRow = 1,
  startCol = 1
)
}
\arguments{
\item{wb}{An openxlsx workbook object created with openxlsx::createWorkbook()}

\item{sheet_name}{Character string specifying the name of the worksheet to create}

\item{data}{A data frame or matrix containing the data to write to the worksheet}

\item{colNames}{Logical indicating whether to write column names. Default is TRUE}

\item{na_as_string}{Logical indicating how to handle NA values:
- If TRUE, NA values are written as the string "NA" in Excel cells
- If FALSE, NA values are written as empty cells in Excel
Default is FALSE (empty cells)}

\item{startRow}{Integer specifying the row number to start writing data. Default is 1}

\item{startCol}{Integer specifying the column number to start writing data. Default is 1}
}
\value{
None. The function modifies the workbook object in place by adding a 
  new worksheet with the specified data.
}
\description{
This helper function adds a new worksheet to an openxlsx workbook object and 
writes data to it with customizable handling of NA values. It provides a 
convenient wrapper around openxlsx::addWorksheet() and openxlsx::writeData() 
with standardized formatting.
}
\details{
The function creates a worksheet with gridLines enabled by default. When 
na_as_string = FALSE, the function uses keepNA = FALSE to ensure NA values 
appear as empty cells rather than displaying any text. This is particularly 
useful for detectability and quantifiability table where empty cells are shown as "NA" text.
}
\seealso{
\code{\link[openxlsx]{addWorksheet}}, \code{\link[openxlsx]{writeData}}
}
\keyword{internal}
