% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mergeNULISAseq.R
\name{clean_covariate_names}
\alias{clean_covariate_names}
\title{This helper function applies `janitor::make_clean_names` to a character vector of column names, except for those in the protected columns list. Used to ensure column names are syntactically valid and consistent for downstream processing.}
\usage{
clean_covariate_names(x, ...)
}
\arguments{
\item{x}{Character vector of column names to clean.}

\item{...}{Additional arguments PASS to `janitor::make_clean_names` (e.g., `case = 'lower_camel'`).}
}
\value{
Character vector of cleaned column names, with protected columns left unchanged.
}
\description{
This helper function applies `janitor::make_clean_names` to a character vector of column names, except for those in the protected columns list. Used to ensure column names are syntactically valid and consistent for downstream processing.
}
\details{
- Protected columns (see `protected_columns()`) are not altered.
- Used internally to standardize column names in NULISAseq sample data frames.
}
\examples{
clean_covariate_names(c("Sample Name", "plateID", "Well Row"), case = "lower_camel")

}
\keyword{internal}
